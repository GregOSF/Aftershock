<div class="container">
	<div class="row">
		<div class="col-md-6 col-md-offset-3 text-center">
			<div class="quakeListContainer">
			<h2 class="quakeListTitleCon"> Be Prepared When An Earthquake Hits </h2>
			
			
			<hr>
			<% @prequakeseed_todos.each do |prequakeseedtodo| %>
        <p>
          <%= prequakeseedtodo.desc %>
        </p>
        
        <hr>
      <% end %>
			
			<% if current_user %> 
				<% @todos.each do |todos| %>
					<p>
						<%= todos.desc %>
					</p>
				
				<% end %>			
			</div>
			<% else %> 
				<%= link_to "Login to Customize Your Pre-Quake List", 'login' , class: 'btn btn-primary' %>
				<%= link_to "Signup to Aftershock to Customize Your Pre-Quake List", 'signup', class: 'btn btn-primary' %>
			<% end %>	

		</div>
	</div>
</div>